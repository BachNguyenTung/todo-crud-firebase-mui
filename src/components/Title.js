import { Box } from "@mui/material";
import React from "react";

const Title = () => {
  return (
    <Box component="h1" width="100%" pl={4} pr={4} sx={{ fontSize: "2rem" }}>
      Todo App
    </Box>
  );
};

export default Title;
